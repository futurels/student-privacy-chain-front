# AGENTS.md

项目基本规范
1.项目中的标识，提示应全部使用中文

## 当前阶段：第五阶段（数据存证闭环页面）

### 当前阶段目标
本阶段只实现：
- P07 提交存证申请
- P08 存证申请/记录列表
- P09 存证详情与一致性校验
- P14 存证审批详情

### 本阶段允许调用的接口
- API-21 ~ API-30

### 强约束
1. 所有请求统一走 API 封装层。
2. 申请页必须展示摘要信息和申请说明输入区。
3. 列表页必须使用页签 + 筛选栏 + 表格 + 分页结构。
4. 详情页必须展示摘要、txId、blockHash、状态、校验结果。
5. 审批页必须支持通过/驳回，并填写审核意见。
6. 不允许提前开发授权共享、审计追溯、区块链综合查询页面。
7. 页面优先可用和可联调，不做复杂链上可视化。
8. 代码保证良好的健壮性和可读性，关键代码需有详细注释。

### 输出要求
每次任务输出必须包含：
1. 新增页面/组件清单
2. 接口联调清单
3. 自测方法
4. 未完成项 / 风险点