## 当前阶段：第四阶段（文件上传与附件管理页面）

在开发前先阅读docs目录下的相关文档再进行分析开发

### 当前阶段目标
本阶段只实现：
- P06 文件上传与附件管理
- P04 隐私数据详情中的附件区域
- P05 新增/编辑隐私数据中的附件区域

### 本阶段允许调用的接口
- API-31 ~ API-34
- 如后端已提供，可补 API-35、API-36

### 强约束
1. 所有请求统一走 API 封装层。
2. 上传区必须展示进度、状态、失败提示。
3. 文件列表必须展示摘要和 CID。
4. 下载/预览前必须有基础确认提示。
5. 不允许提前开发存证申请、授权申请、审计、区块链查询相关页面与联调。
6. P04/P05 中后续阶段按钮只允许做占位或禁用态。
7. 页面优先可用和可联调，不追求复杂文档预览能力。


### 输出要求
每次任务输出必须包含：
1. 新增页面/组件清单
2. 接口联调清单
3. 自测方法
4. 未完成项 / 风险点