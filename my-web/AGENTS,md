## 当前阶段：第六阶段-第一小段（授权申请页面）

### 当前阶段目标
本小段只实现：
- P10 发起共享授权
- P11 申请列表与申请详情

### 本小段允许调用的接口
- API-37
- API-38
- API-39

### 强约束
1. P10 只负责提交授权申请，不负责审批或授权生效。
2. P11 只展示申请列表和申请详情，不展示有效授权/历史授权真实数据。
3. 所有请求统一走 API 封装层。
4. 表单字段必须严格对齐后端接口。
5. 不允许提前开发 P12、P15、撤销按钮、访问记录、审批时间线等后续功能。
6. 页面优先可用和可联调，不做复杂流程可视化。

### 输出要求
每次任务输出必须包含：
1. 新增页面/组件清单
2. 接口联调清单
3. 自测方法
4. 未完成项 / 风险点

## 当前阶段：第六阶段-第二小段（授权审批页面）

### 当前阶段目标
本小段只实现：
- P13 授权待办入口
- P15 授权审批详情
- P11 申请列表状态刷新

### 本小段允许调用的接口
- API-38
- API-39
- API-40
- API-41

### 强约束
1. P13 只展示授权待办，不展示有效授权和历史授权。
2. P15 必须支持通过/驳回两种审批动作。
3. 驳回时必须输入驳回原因。
4. 学生端 P11 只验证状态刷新，不新增后续授权操作入口。
5. 不允许提前开发有效授权列表、访问记录、撤销授权、到期回收相关页面。
6. 页面优先可用和可联调，不做复杂流程可视化。

### 输出要求
每次任务输出必须包含：
1. 新增页面/组件清单
2. 接口联调清单
3. 自测方法
4. 未完成项 / 风险点